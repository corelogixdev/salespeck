<!doctype html>
<html data-layout="horizontal" data-topbar="light" data-sidebar="light" data-sidebar-size="sm-hover"
    data-sidebar-image="none" data-preloader="disable">

<head>
    <meta charset="utf-8" />
    <title>SalesPeck</title>
    <link rel="icon" href="/assets/img/icon.png" type="image/x-icon">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="/assets/css/vendor/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/vendor/icons.min.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/vendor/app.min.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/custom.min.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/toastr.min.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/jquery-confirm.min.css" rel="stylesheet" type="text/css" />
    <script src="/assets/js/vendor/jquery.min.js"></script>
    <script src="/assets/js/jquery-confirm.min.js"></script>
    <script src="/assets/js/vendor/select2.min.js"></script>
    <script src="/assets/libs/flatpickr/flatpickr.min.js"></script>
    <link href="/assets/css/pos-theme.css" rel="stylesheet" type="text/css" />
    <link href="/assets/css/pos-pages.css" rel="stylesheet" type="text/css" />
    <style>
        @media print {
            .no-print { display: none !important; }
            .duplicate-watermark {
                color: #e57373 !important;
                border-color: #e57373 !important;
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
                opacity: 0.65 !important;
            }
        }
        .duplicate-watermark {
            display: none;
            position: absolute;
            top: 35%;
            left: 8%;
            right: 8%;
            text-align: center;
            transform: rotate(-25deg);
            font-size: 3.5rem;
            font-weight: 900;
            color: #e57373 !important;
            border: 5px dashed #e57373 !important;
            padding: 12px 24px;
            pointer-events: none;
            text-transform: uppercase;
            letter-spacing: 5px;
            z-index: 999;
            -webkit-print-color-adjust: exact !important;
            print-color-adjust: exact !important;
            opacity: 0.65;
        }
        .show-duplicate .duplicate-watermark {
            display: block !important;
        }
    </style>
</head>

<body>

    <div class="row mx-auto cstm-row mt-3 px-4">
        <div class="col-12">
            <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                <h4 class="mb-sm-0">Invoice Details</h4>
                <div class="page-title-right">
                    <ol class="breadcrumb m-0">
                        <li class="breadcrumb-item"><a href="/sales">Sales</a></li>
                        <li class="breadcrumb-item active">Invoice Details</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <div class="row px-4 pb-4">
        <div class="col-lg-12">
            <div class="card position-relative" id="demo">
                <div class="duplicate-watermark" id="duplicateWatermark">DUPLICATE INVOICE</div>
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card-header border-bottom-dashed px-4">
                            <img src="/assets/img/icon.png" class="card-logo mx-auto mb-3 d-block" alt="logo" height="50">
                            <div class="d-flex justify-content-between">
                                <div class="flex-grow-1">
                                    <div class="mt-1">
                                        <h6 class="text-uppercase fw-bold">Company Details</h6>
                                        <p class="mb-1">
                                            <%= companySettings.name %>
                                        </p>
                                        <p class="mb-0">
                                            <%= companySettings.address %>
                                        </p>
                                        <p class="mb-0">Contact: <%= companySettings.phone %>
                                        </p>
                                    </div>
                                </div>
                                <% if(sale.Customer) { %>
                                    <div class="flex-shrink-0">
                                        <h6 class="text-uppercase fw-bold mt-1">Vendor Details</h6>
                                        <p class="mb-2">
                                            <%= sale.Customer.firstname %>
                                                <%= sale.Customer.lastname %>
                                        </p>
                                        <p class="mb-1">
                                            <%= sale.Customer.address %>
                                        </p>
                                        <p class="mb-1">Phone: <%= sale.Customer.phone %>
                                        </p>
                                    </div>
                                    <% } %>
                            </div>
                        </div>

                        <div class="card-body pt-4 pb-0 px-4">
                            <div class="row g-3">
                                <div class="col-4">
                                    <p class="mb-2 text-uppercase fw-bold">
                                        Invoice No
                                    </p>
                                    <h5 class="fs-14 mb-0">
                                        <%= sale.invoicenum %>
                                    </h5>
                                </div>
                                <div class="col-4">
                                    <p class="mb-2 text-uppercase fw-bold">
                                        Date
                                    </p>
                                    <h5 class="fs-14 mb-0 format-date">
                                        <%= sale.createdAt %>
                                    </h5>
                                </div>
                                <div class="col-4">
                                    <p class="mb-2 text-uppercase fw-bold">
                                        Total Amount
                                    </p>
                                    <h5 class="fs-14 mb-0">
                                        Rs.<%= sale.formattedNetAmount %>
                                    </h5>
                                </div>
                            </div>
                        </div>

                        <div class="card-body py-4 px-3 fs-6">
                            <div class="table-responsive">
                                <table class="table table-borderless text-center table-nowrap align-middle mb-0">
                                    <thead>
                                        <tr class="table-active">
                                            <th scope="col" style="width: 50px;">#</th>
                                            <th scope="col">Product</th>
                                            <th scope="col">Rate</th>
                                            <th scope="col">Qty</th>
                                            <th scope="col" class="text-end">Amount</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% sale.SoldPoducts.forEach((product, index)=> { %>
                                            <tr>
                                                <th scope="row">
                                                    <%= index + 1 %>
                                                </th>
                                                <td class="text-center"
                                                    style="max-width: 8mm; overflow-wrap: break-word; white-space: unset;">
                                                    <span class="fw-medium">
                                                        <%= product.Product.name %>
                                                    </span>
                                                </td>
                                                <td class="code">
                                                    <span>
                                                        Rs.<%= product.price %>
                                                    </span>
                                                </td>
                                                <td class="code">
                                                    <%= product.quantity %>
                                                </td>
                                                <td class="text-end code">Rs.<%= (product.quantity * product.price
                                                        || 0).toFixed(2) %>
                                                </td>
                                            </tr>
                                            <% }); %>
                                    </tbody>
                                </table>
                            </div>

                            <div class="border-top border-top-dashed mt-2 fs-7">
                                <table class="table table-borderless table-nowrap align-middle mb-0 ms-auto footer-table" style="width: auto; min-width: 280px;">
                                    <tbody>
                                        <tr>
                                            <td class="p-1 text-end">Sub Total</td>
                                            <td class="text-end code p-1">Rs.<%= sale.formattedSubTotal %></td>
                                        </tr>
                                        <% if(sale.discountPercentage > 0) { %>
                                            <tr>
                                                <td class="p-1 text-end text-danger">Discount (<%= sale.formattedDiscountPercentage %>%)</td>
                                                <td class="text-end code p-1 text-danger">- Rs.<%= sale.formattedDiscountAmount %></td>
                                            </tr>
                                        <% } %>
                                        <tr class="border-top border-top-dashed fs-15 fw-bold">
                                            <th scope="row" class="p-1 text-end">Total</th>
                                            <th class="text-end code p-1">Rs.<%= sale.formattedNetAmount %></th>
                                        </tr>
                                        <tr>
                                            <td class="fw-bold p-1 text-end">Paid</td>
                                            <td class="text-end code p-1">Rs.<%= sale.formattedTotalPayment %></td>
                                        </tr>
                                        <% if(sale.balance > 0) { %>
                                            <tr>
                                                <th class="p-1 text-end text-warning">Balance Due</th>
                                                <th class="text-end code p-1 text-warning">Rs.<%= sale.formattedBalance %></th>
                                            </tr>
                                        <% } else if(sale.change > 0) { %>
                                            <tr>
                                                <th class="p-1 text-end text-success">Change / Excess</th>
                                                <th class="text-end code p-1 text-success">Rs.<%= sale.formattedChange %></th>
                                            </tr>
                                        <% } %>
                                        <% if (typeof customerBalance !== 'undefined' && customerBalance !== null) { %>
                                            <tr id="print-balance-row" class="d-none border-top border-top-dashed">
                                                <th class="p-1 text-end">Current Balance</th>
                                                <th class="text-end code p-1">Rs.<%= customerBalance.toLocaleString(undefined, { minimumFractionDigits: 2 }) %></th>
                                            </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                                <p class="text-end border-top pt-2">
                                    <small class="text-muted">
                                        Powered by <strong>SalesPeck</strong>
                                    </small>
                                </p>

                            </div>

                            <div class="d-flex justify-content-between align-items-center mt-4 no-print">
                                <div class="d-flex align-items-center gap-3">
                                    <% if (typeof customerBalance !== 'undefined' && customerBalance !== null) { %>
                                    <div class="form-check form-switch form-switch-success form-switch-md">
                                        <input class="form-check-input" type="checkbox" role="switch" id="showBalanceToggle">
                                        <label class="form-check-label fs-13" for="showBalanceToggle">Show Current Balance on Print</label>
                                    </div>
                                    <% } %>
                                    <div class="form-check form-switch form-switch-danger form-switch-md">
                                        <input class="form-check-input" type="checkbox" role="switch" id="duplicateToggle">
                                        <label class="form-check-label fs-13 fw-semibold text-danger" for="duplicateToggle">
                                            <i class="ri-file-copy-2-line me-1"></i>Print as Duplicate Invoice
                                        </label>
                                    </div>
                                </div>
                                <div class="d-flex gap-2">
                                    <% if(typeof hidenav === 'undefined' || !hidenav) { %>
                                    <button onclick="window.history.back()" class="btn btn-light">Back</button>
                                    <% } %>
                                    <a href="/sales/returns?saleId=<%= sale.id %>" class="btn btn-soft-warning">
                                        <i class="ri-arrow-go-back-line align-bottom me-1"></i> Create Return
                                    </a>
                                    <button onclick="printReceipt()" class="btn btn-success">
                                        <i class="ri-printer-line align-bottom me-1"></i> Print
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
<script src="/assets/js/vendor/moment.min.js"></script>
<script>
    // Printer configuration from settings
    const printerConfig = <%- JSON.stringify(typeof printerConfig !== 'undefined' ? printerConfig : { silentPrinting: false, numberOfPrints: 1 }) %>;
    
    function markInvoicePrinted() {
        try {
            const data = JSON.stringify({ type: 'sale', id: '<%= sale.id %>' });
            if (navigator.sendBeacon) {
                const blob = new Blob([data], { type: 'application/json' });
                navigator.sendBeacon('/invoices/mark-printed', blob);
            } else {
                fetch('/invoices/mark-printed', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: data,
                    keepalive: true
                }).catch(err => console.error('Error marking invoice printed:', err));
            }
        } catch(e) {
            console.error(e);
        }
    }

    window.addEventListener('beforeprint', markInvoicePrinted);

    function printReceipt() {
        markInvoicePrinted();
        const numberOfPrints = parseInt(printerConfig.numberOfPrints || 1);
        const silentPrinting = printerConfig.silentPrinting === true || printerConfig.silentPrinting === 'true';
        
        function doPrint() {
            if (silentPrinting) {
                const iframe = document.createElement('iframe');
                iframe.style.position = 'fixed';
                iframe.style.right = '0';
                iframe.style.bottom = '0';
                iframe.style.width = '0';
                iframe.style.height = '0';
                iframe.style.border = '0';
                document.body.appendChild(iframe);
                
                const printContent = document.getElementById('demo').innerHTML;
                const printWindow = iframe.contentWindow;
                printWindow.document.open();
                let styles = '';
                Array.from(document.querySelectorAll('style')).forEach(styleEl => {
                    styles += styleEl.innerHTML + '\n';
                });
                
                const isDuplicateActive = document.getElementById('demo').classList.contains('show-duplicate');
                printWindow.document.write(`
                    <!DOCTYPE html>
                    <html>
                    <head>
                        <title>Print Receipt</title>
                        <style>
                            ${styles}
                            @media print {
                                .no-print { display: none !important; }
                                .duplicate-watermark {
                                    color: #e57373 !important;
                                    border-color: #e57373 !important;
                                    -webkit-print-color-adjust: exact !important;
                                    print-color-adjust: exact !important;
                                    opacity: 0.65 !important;
                                }
                            }
                        </style>
                    </head>
                    <body class="${isDuplicateActive ? 'show-duplicate' : ''}">
                        <div id="demo" class="card position-relative ${isDuplicateActive ? 'show-duplicate' : ''}">
                            ${printContent}
                        </div>
                    </body>
                    </html>
                `);
                printWindow.document.close();
                
                printWindow.onload = function() {
                    setTimeout(() => {
                        printWindow.focus();
                        printWindow.print();
                        setTimeout(() => {
                            if (document.body.contains(iframe)) {
                                document.body.removeChild(iframe);
                            }
                        }, 1000);
                    }, 100);
                };
            } else {
                window.print();
            }
        }
        
        if (numberOfPrints > 1) {
            let printCount = 0;
            const printInterval = setInterval(() => {
                doPrint();
                printCount++;
                if (printCount >= numberOfPrints) {
                    clearInterval(printInterval);
                }
            }, silentPrinting ? 1500 : 500);
        } else {
            doPrint();
        }
    }

    $(document).ready(function () {
        setTimeout(() => {
            $('.format-date').each(function () {
                var date = $(this).text().trim();
                $(this).text(moment(new Date(date)).format('MMMM Do YYYY'));
            });
        });

        $('#showBalanceToggle').on('change', function() {
            if ($(this).is(':checked')) {
                $('#print-balance-row').removeClass('d-none');
            } else {
                $('#print-balance-row').addClass('d-none');
            }
        });

        $('#duplicateToggle').on('change', function() {
            if ($(this).is(':checked')) {
                $('#demo').addClass('show-duplicate');
            } else {
                $('#demo').removeClass('show-duplicate');
            }
        });

        // Auto check duplicate toggle if query param duplicate=true or printCount > 0
        const urlParams = new URLSearchParams(window.location.search);
        const isDuplicateParam = urlParams.get('duplicate') === 'true';
        const hasBeenPrintedBefore = <%= (sale.printCount > 0) ? 'true' : 'false' %>;

        if (isDuplicateParam || hasBeenPrintedBefore) {
            $('#duplicateToggle').prop('checked', true);
            $('#demo').addClass('show-duplicate');
        }
    });
</script>

</html>
