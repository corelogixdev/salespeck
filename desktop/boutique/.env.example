# Build-machine only — never commit. Excluded from the installer.
# Create a GitLab personal access token with api / write_package_registry scope.
GITLAB_TOKEN=glpat-xxxxxxxx

# Optional for local Prisma tools (app also resolves SQLite itself)
# DATABASE_URL=file:./db/stitch.sqlite
